import {} from "@faremeter/types/x402";
export function addX402PaymentRequirementDefaults(req) {
    // These come from coinbase/x402's defaults.
    req.description ??= "";
    req.mimeType ??= "application/json";
    return req;
}
