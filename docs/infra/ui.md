# Mission Console UI (Public Summary)

The React/Vite front end renders Digital IDs, MCP prompts, and telemetry in a translucent mission-console shell. Implementation details (component tree, theming, state flows) live in the private repo; schedule a call to view the full spec.
